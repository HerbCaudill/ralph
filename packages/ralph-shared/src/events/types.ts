/**
 * Event-related types defined by ralph-shared.
 *
 * Core event types (MessageEventType, ToolUseEventType, etc.) are defined in
 * @herbcaudill/agent-view. Backward-compatible aliases (AgentMessageEvent, etc.)
 * are defined in @herbcaudill/agent-server/agentTypes.
 *
 * This module is intentionally empty after removing the re-exports. It is kept
 * as a placeholder in case ralph-shared needs to define its own event-related
 * types in the future.
 */
