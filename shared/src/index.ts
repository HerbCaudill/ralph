/**
 * @herbcaudill/ralph-shared
 *
 * Shared utilities and types for Ralph CLI and UI packages.
 *
 * This package is currently a placeholder and will be populated with:
 * - Agent event types and normalization
 * - Beads domain types
 * - Prompt loader utilities
 * - Other shared code
 */

// Placeholder export to ensure the package builds correctly
export const VERSION = "0.8.5"
