{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Hotkeys Configuration",
  "description": "Keyboard hotkeys configuration for ralph-ui using VS Code key binding format",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "hotkeys": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/hotkey"
      }
    }
  },
  "required": ["hotkeys"],
  "definitions": {
    "hotkey": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "VS Code-style key binding (e.g., 'cmd+shift+t', 'ctrl+c', 'Enter'). Modifiers: cmd, ctrl, alt, shift. Use '+' to combine."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the hotkey action"
        },
        "category": {
          "type": "string",
          "description": "Category for grouping hotkeys in the keyboard shortcuts dialog"
        }
      },
      "required": ["key", "description", "category"]
    }
  }
}
